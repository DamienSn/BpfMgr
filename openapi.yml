openapi: '3.0.2'
info:
  title: API Title
  version: '1.0'
servers:
  - url: https://api.server.test/v1
paths:
  /test:
    get:
      responses:
        '200':
          description: OK
  /bpfs:
    get:
      summary: 'BPFS: Retourne un liste de bpf'
      parameters:
        - name: id
          in: query
          schema:
            type: integer
        - name: dpt
          in: query
          schema:
            type: number
        - name: name
          in: query
          schema:
            type: string
        